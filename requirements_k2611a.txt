# Keithley 2611A Control Suite Dependencies
PyQt5>=5.15.0
pyqtgraph>=0.13.0
pyvisa>=1.13.0
pyvisa-py>=0.7.0
numpy>=1.24.0
Pillow>=10.0.0

# Calorimeter "sniff mode" (passive packet capture instead of connecting).
# The Python side; the kernel driver underneath is a separate manual install:
#   Windows  Npcap from https://npcap.com, "WinPcap API-compatible mode" ticked.
#            Run the app as Administrator.
#   macOS    built in; run with sudo.
#   Linux    built in; run with sudo, or grant CAP_NET_RAW.
scapy>=2.5.0
